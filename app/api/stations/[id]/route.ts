import {NextResponse} from "next/server";import {getStation,getTrainsAtStation} from "@/lib/data";export const runtime="nodejs";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const station=await getStation(id);if(!station)return NextResponse.json({success:false,error:"Station not found"},{status:404});return NextResponse.json({success:true,source:"supabase",station,trains:await getTrainsAtStation(station.id)})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Could not load station"},{status:500})}}
