import {NextResponse} from "next/server";import {listTrains,listStations} from "@/lib/data";export const runtime="nodejs";
export async function GET(request:Request){try{const q=new URL(request.url).searchParams.get("q")?.trim()??"";if(!q)return NextResponse.json({success:true,trains:[],stations:[]});const [trains,stations]=await Promise.all([listTrains(q),listStations(q)]);return NextResponse.json({success:true,trains:trains.slice(0,8),stations:stations.slice(0,8)})}catch(e){return NextResponse.json({success:false,error:e instanceof Error?e.message:"Search failed"},{status:500})}}
