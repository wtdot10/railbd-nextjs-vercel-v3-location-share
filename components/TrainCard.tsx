import Link from "next/link";import type {Train} from "@/lib/data";
export default function TrainCard({train}:{train:Train}){const live=train.updatedAt&&Date.now()-new Date(train.updatedAt).getTime()<10*60*1000;return <Link href={`/trains/${train.number}`} className="card"><div className="top"><span className={`badge ${train.active?"":"inactive"}`}>● {train.active?"Active":"Inactive"}</span><span className="number">{train.number}</span></div><div className="trainName">{train.name}</div><div className="number">{train.name_bn??""}</div><div className="routeLine"><span>{train.from??"Origin unavailable"}</span><span>→</span><span>{train.to??"Destination unavailable"}</span></div>{train.progress!=null?<><div className="progress"><i style={{width:`${train.progress}%`}}/></div><div className="route"><span>Live position</span><b>{train.progress}%</b></div></>:<div className="emptyLive">No verified live position</div>}<div className="cardMeta"><span>{train.stationCount} stops</span><span>{train.trainType??"Train"}</span><span>{live?"Updated recently":"Schedule data"}</span></div></Link>}
